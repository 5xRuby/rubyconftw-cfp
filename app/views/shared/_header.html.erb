<nav class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "RubyConfTW CFP", root_path, class: "navbar-brand", :"data-no-turbolink" => true %>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav navbar-left">
        <% if user_signed_in? && current_user.is_admin %>
          <li><%= link_to 'Activities', admin_activities_path%></li>
          <li><%= link_to 'Users', admin_users_path %></li>
          <%= admin_review_paper_link(Activity.recent.first)  %>
        <% end %>

      </ul>
      <ul class="nav navbar-nav navbar-right">


        <% if user_signed_in? %>
          <li><%= link_to "My Proposals", my_proposals_path %></li>
        <% end %>

        <li><%= link_to "Events", root_path %></li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= image_tag(current_user.photo.url, width: 24, height: 24, class: "img-circle") if current_user.photo.present? %>
              <%= current_user.name %>!
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to edit_user_registration_path do %>
                  <span class="fa fa-user"></span>  Profile
                <% end %>
              </li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete do %>
                  <span class="fa fa-sign-out"></span> Sign out
                <% end %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Sign in", new_user_session_path %></li>
        <% end %>

      </ul>
    </div>
  </div>
</nav>
