.col-md-8.col-md-offset-2
  = simple_form_for @activity, url: [:admin, @activity] do |f|
    = f.input :name
    = f.input :logo
    = f.input :description
    = f.input :start_date, as: :string, input_html: {class: "dpicker"}
    = f.input :end_date, as: :string, input_html: {class: "dpicker"}
    = f.input :open_at, as: :string, input_html: {class: "dtpicker"}
    = f.input :close_at, as: :string, input_html: {class: "dtpicker"}
    = f.input :term
    h3 Input Fields
    ul#input_fields_container
      = f.simple_fields_for :custom_fields do |cf|
          = render "custom_field", cf: cf
    .form-group
      a.btn.btn-default#add-field(data-form-content="#{render(partial: "new_field", locals: {f:f, idx: rand(99999) + 1}).gsub(/\n/,"")  }") Add Field

    = f.submit "Save"
